# Description: HuggingFace training configuration (default approach)

model: !include ../shared/huggingface_model_config.yml
dataset: !include ../shared/huggingface_dataset_default.yml
dataloader:
  train: !include ../shared/dataloader_config.yml
  test: !include ../shared/dataloader_config.yml
device: !include ../shared/device_config.yml
training: !include ../shared/training_config.yml
